import {Task} from "./Task";

export class ClassTask implements Task{

    id: string;
    taskName: string;
    description: string;
    completed: boolean;

    // TODO: add methods to add new, delete, complete, display all

}